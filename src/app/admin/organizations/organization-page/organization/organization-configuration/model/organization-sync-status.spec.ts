import { OrganizationSyncStatus } from './organization-sync-status.model';

describe('OrganizationSyncStatus', () => {
  it('should create an instance', () => {
    expect(new OrganizationSyncStatus()).toBeTruthy();
  });
});
