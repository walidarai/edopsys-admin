import { OrganizationLock } from './organization-lock.model';

describe('OrganizationLock', () => {
  it('should create an instance', () => {
    expect(new OrganizationLock()).toBeTruthy();
  });
});
